DROP TABLE IF EXISTS BENCHMARK;
CREATE TABLE BENCHMARK AS 
SELECT DISTINCT
    CALDT,
    TICKER,
    PRICE AS ENDING_VALUE
FROM POSITIONS
WHERE TICKER = 'IWV'
AND FUND = 'undergrad'
ORDER BY CALDT DESC;
