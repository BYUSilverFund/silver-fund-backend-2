INSERT INTO BENCHMARK
    (
        CALDT,
        TICKER,
        ENDING_VALUE
    )
SELECT
    CALDT,
    TICKER,
    ENDING_VALUE
FROM "{{xf_table}}"
ON CONFLICT (CALDT)
DO UPDATE SET
    CALDT = EXCLUDED.CALDT,
    TICKER = EXCLUDED.TICKER,
    ENDING_VALUE = EXCLUDED.ENDING_VALUE
;
