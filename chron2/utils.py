import pandas as pd

# Declare columns to keep here

def clean_ibkr_dataframe(dataframe: pd.DataFrame, query, fund):
    # Remove extra header rows
    # Keep only specified columns
    # Add fund column
    # Add date column (using other column in dataframe)

    pass

def render_template(sql_file, params):
    # Take template file and render it with the given params using jinja2
    pass